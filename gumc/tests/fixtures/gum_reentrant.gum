use gum.defaults.Account

interface ICallback:
    fn ping() -> bool

contract V:
    u256 counter

    export {MOD}fn poke(Account cb):
        V.counter = V.counter + 1
        assert(ICallback(cb).ping(), "cb failed")
