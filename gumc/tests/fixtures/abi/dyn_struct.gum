use gum.defaults.String

class Meta:
    u256 id
    String name

    fn new(self, u256 i, String n):
        self.id = i
        self.name = n

contract C:
    export fn echo(Meta m) -> Meta:
        return m
